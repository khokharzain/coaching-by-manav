# Coaching by Manav
#
# Cloudflare appends its own managed block above this one, covering AI
# training crawlers. It appends rather than replaces, so the Sitemap
# line below survives. Change the managed block under
# Cloudflare > the zone > AI Crawl Control, not here.

User-agent: *
Allow: /

Sitemap: https://coachingbymanav.com.au/sitemap.xml
